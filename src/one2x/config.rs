//! One2X configuration types.
//!
//! The canonical `WebChannelConfig` is defined in `zeroclaw-config` (under
//! the `one2x` feature flag). This module re-exports it for convenience.

pub use zeroclaw_config::scattered_types::WebChannelConfig;
