apiVersion: v1
kind: ConfigMap
metadata:
  name: daemonclaw-config
  namespace: daemonclaw
  labels:
    app.kubernetes.io/name: daemonclaw
    app.kubernetes.io/part-of: daemonclaw
data:
  config.toml: |
    workspace_dir = "/daemonclaw-data/workspace"
    config_path = "/daemonclaw-data/.daemonclaw/config.toml"
    default_provider = "anthropic"
    default_model = "claude-sonnet-4-20250514"
    default_temperature = 0.7

    [gateway]
    port = 42617
    host = "[::]"
    allow_public_bind = true
    require_pairing = true
    web_dist_dir = "/daemonclaw-data/web/dist"

    [autonomy]
    level = "supervised"
    auto_approve = ["file_read", "file_write", "file_edit", "memory_recall", "memory_store", "web_search_tool", "web_fetch", "calculator", "glob_search", "content_search", "image_info", "weather", "git_operations"]
