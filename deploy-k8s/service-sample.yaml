apiVersion: v1
kind: Service
metadata:
  name: daemonclaw
  namespace: daemonclaw
  labels:
    app.kubernetes.io/name: daemonclaw
    app.kubernetes.io/part-of: daemonclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: daemonclaw
  ports:
    - name: gateway
      port: 42617
      targetPort: gateway
      protocol: TCP
