{
  "name": "zeroclaw-browser-scripts",
  "version": "2.0.0",
  "private": true,
  "description": "Browser automation daemon for MoA (persistent Playwright, gstack-inspired)",
  "scripts": {
    "install-browser": "npx playwright install chromium",
    "daemon": "node playwright-daemon.js",
    "daemon:headed": "node playwright-daemon.js --headed"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
