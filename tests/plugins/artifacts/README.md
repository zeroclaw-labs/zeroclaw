# Test Plugin Artifacts

This directory contains compiled `.wasm` artifacts for integration testing.

Artifacts are built via `build-test-plugins.sh` at the repository root.
To rebuild:

```sh
./build-test-plugins.sh
```
