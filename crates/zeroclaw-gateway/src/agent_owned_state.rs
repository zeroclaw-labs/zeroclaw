//! Re-export of the shared agent owned-state cascade so gateway paths keep
//! resolving; the logic lives in `zeroclaw_runtime::config_ops`.

pub use zeroclaw_runtime::config_ops::agent_owned_state::*;
