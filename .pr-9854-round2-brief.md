Round 2 on zeroclaw PR #9854. You are in
`~/git/zeroclaw-worktrees/zerorouter-context-window` on
`fix/zerorouter-context-window-autodetect` at `a283af4c0`. Audacity88
re-reviewed and resolved ALL blockers; approval now waits on two warnings.
Read the review: `gh pr view 9854 --repo zeroclaw-labs/zeroclaw --json
reviews --jq '.reviews[-1].body'`.

Same rules as before: gate = `cargo nextest run --locked -p
zeroclaw-providers`, clippy `-D warnings`, fmt, comment hygiene script;
mutation-check new guards; no AI trailer; commit but do NOT push.

## 1. Make malformed Zhipu credentials fail closed (the reviewer offered
##    narrowing the claims instead — take the fix, it is strictly better)

`apply_auth_to_request`'s `ZhipuJwt` arm falls back to sending the stored
credential as plain bearer when JWT minting fails
(`compatible.rs`, the `Err(_) => bearer` branch). That is the exact leak
class this PR exists to prevent, surviving in the shared helper. Make it
fail closed: a malformed `id.secret` produces an error (or an
unauthenticated-request refusal — pick what the call sites can propagate
cleanly and say why), never the raw secret on the wire. Cover the branch
with a wire-level regression: malformed credential → no request carrying
the stored value leaves the client. Check every call site's error path so a
chat request with a malformed credential gets a clear local error instead
of a confusing upstream 401 — that behavior change is the point, name it in
the commit message.

## 2. Narrow the absolute claims to what the head proves

With fail-closed in place, re-audit every comment and test description this
PR added for absolute guarantees ("every credential-bearing path", "can
never reach the wire") and narrow each to what is actually proven: excluded
Zhipu families make no discovery request; a valid id.secret is transformed;
a malformed one now fails closed. Other providers' independent auth paths
are out of this PR's scope — the comments must not annex them.

## 3. Draft the corrected PR body

The PR body still describes the superseded blanket-opt-in design. Write the
corrected body to `.pr-9854-body.md` in the worktree (do not edit the PR
yourself): current design (explicit opt-in, default false, eligible set
unchanged, tests prove exclusion), and state plainly that Crusoe (#9338)
and ZeroRouter (#9645) each need their own one-line catalog declaration.

## Report

Orchestrator input: the fail-closed design and file:line, call-site error
propagation, gate output, mutation results, what you narrowed where.
Commit, no push.
