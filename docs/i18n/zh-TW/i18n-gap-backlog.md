# i18n 缺口待辦清單（繁體中文）

本檔案追蹤在地化同等性缺口與修復狀態。

最近更新日期：**2026-02-24**。

## 基準線定義

缺口基準線 = `docs/*.md` 下的頂層英文文件集合（排除 README/SUMMARY 語系變體及舊版 `*.vi.md` 墊片），與 `docs/i18n/<locale>/` 進行比對。

## 目前缺口數量

| 語系 | 缺少頂層文件同等數量 | 目前狀態 |
|---|---:|---|
| `zh-CN` | 0 | 完整頂層同等（橋接 + 在地化） |
| `zh-TW` | 0 | 完整頂層同等（橋接 + 在地化，從 zh-CN 轉換） |
| `ja` | 0 | 完整頂層同等（橋接 + 在地化） |
| `ru` | 0 | 完整頂層同等（橋接 + 在地化） |
| `fr` | 0 | 完整頂層同等（橋接 + 在地化） |
| `vi` | 0 | 完整頂層同等 |
| `el` | 0 | 完整頂層同等 |

## 修復紀錄（2026-02-24）

在此 PR 流程中完成：

- Wave 1 執行期在地化，涵蓋 `zh-CN`/`ja`/`ru`/`fr`：
  - `commands-reference.md`
  - `providers-reference.md`
  - `channels-reference.md`
  - `config-reference.md`
  - `operations-runbook.md`
  - `troubleshooting.md`
- 透過在地化橋接頁面完成 `zh-CN`/`ja`/`ru`/`fr` 剩餘頂層文件的完整覆蓋。
- `vi` 與 `el` 已維持完整頂層同等。

## 剩餘缺口（基準線範圍）

- 無。所有支援語系的頂層基準線缺口均已關閉。

## 選用的進階深度

以下並非基準線阻擋項目，但可在未來波次中推進：

- 針對 `zh-CN`/`ja`/`ru`/`fr` 中的 33 份增強型橋接頁面，提升至完整敘述翻譯深度
- 在不同 provider/channel 環境中，當操作行為有所差異時，提供語系專屬範例（指令/設定片段）

## 追蹤規則

1. 本檔案須加註日期戳記，並在重要修復檢查點時追加記錄。
2. 新增頂層英文文件時，重新執行同等性數量計算，並在同一 PR 中更新本檔案。
3. 新增或移除語系時，保持語系導航同等性完整。
