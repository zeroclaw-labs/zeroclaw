apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: signal-cli-daemon-data
  namespace: ai-agents
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-client
