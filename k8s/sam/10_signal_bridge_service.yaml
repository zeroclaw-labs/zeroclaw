apiVersion: v1
kind: Service
metadata:
  name: signal-bridge
  namespace: ai-agents
  labels:
    app: signal-bridge
spec:
  selector:
    app: signal-bridge
  ports:
    - name: http
      port: 8686
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
