apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zeroclaw-config
  namespace: zeroclaw-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
