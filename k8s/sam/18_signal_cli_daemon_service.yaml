apiVersion: v1
kind: Service
metadata:
  name: signal-cli-daemon
  namespace: zeroclaw-test
  labels:
    app: signal-cli-daemon
spec:
  selector:
    app: signal-cli-daemon
  ports:
    - name: http
      port: 8686
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
