apiVersion: v1
kind: Service
metadata:
  name: zeroclaw-k8s-agent
  namespace: ai-agents
spec:
  selector:
    app: zeroclaw-k8s-agent
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  type: ClusterIP
