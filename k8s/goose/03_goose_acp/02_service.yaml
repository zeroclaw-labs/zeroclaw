apiVersion: v1
kind: Service
metadata:
  name: goose-acp
  namespace: ai-agents
spec:
  selector:
    app: goose-acp
  ports:
    - name: acp-http
      port: 8080
      targetPort: acp-http
      protocol: TCP
  type: ClusterIP
