# Zeroclaw K8s Infrastructure Agent — replaces Goose ACP.
#
# Runs as an ACP-over-HTTP server (JSON-RPC 2.0 + SSE) that Sam can
# delegate Kubernetes tasks to. Unlike Goose, this agent persists
# conversation history across prompts and uses Serena for cross-session
# memory.
#
# ACP Protocol:
#   POST   /acp — JSON-RPC requests (initialize, session/new, session/prompt)
#   DELETE /acp — Tear down session
#
# In-cluster endpoint:
#   http://zeroclaw-k8s-agent.ai-agents.svc.cluster.local:3000/acp
---
apiVersion: agents.x-k8s.io/v1alpha1
kind: Sandbox
metadata:
  name: zeroclaw-k8s-agent
  namespace: ai-agents
spec:
  replicas: 1
  podTemplate:
    metadata:
      labels:
        app: zeroclaw-k8s-agent
        agent-role: k8s-infrastructure
        protocol: acp-http
    spec:
      serviceAccountName: goose-todolist-configurator
      automountServiceAccountToken: true
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        runAsNonRoot: false
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: regcred
      initContainers:
        - name: install-kubectl
          image: alpine:3.21
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -ec
            - |
              mkdir -p /data/bin
              KUBECTL_VERSION="v1.32.3"
              if [ -x /data/bin/kubectl ]; then
                echo "kubectl already installed"
              else
                echo "Installing kubectl ${KUBECTL_VERSION}..."
                ARCH=$(uname -m | sed 's/x86_64/amd64/' | sed 's/aarch64/arm64/')
                wget -qO /data/bin/kubectl "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/${ARCH}/kubectl"
                chmod +x /data/bin/kubectl
                echo "kubectl installed"
              fi
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              memory: 128Mi
          volumeMounts:
            - name: zeroclaw-k8s-data
              mountPath: /data
        - name: init-config
          image: debian:bookworm-slim
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -ec
            - |
              mkdir -p /data/.zeroclaw /data/.zeroclaw/workspace /data/.kube
              # Sync config.toml from ConfigMap.
              echo "Syncing config.toml"
              cp /etc/zeroclaw-template/config.toml /data/.zeroclaw/config.toml
              chmod 600 /data/.zeroclaw/config.toml
              # Seed identity files into workspace if missing.
              for f in IDENTITY.md TOOLS.md AGENTS.md; do
                if [ ! -f "/data/.zeroclaw/workspace/$f" ] && [ -f "/etc/zeroclaw-template/identity/$f" ]; then
                  echo "Seeding $f"
                  cp "/etc/zeroclaw-template/identity/$f" "/data/.zeroclaw/workspace/$f"
                fi
              done
              # Generate kubeconfig using auto-refreshing SA token.
              cat > /data/.kube/config <<'KUBEEOF'
              apiVersion: v1
              kind: Config
              clusters:
                - cluster:
                    certificate-authority: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
                    server: https://kubernetes.default.svc
                  name: in-cluster
              contexts:
                - context:
                    cluster: in-cluster
                    namespace: todolist
                    user: k8s-agent
                  name: todolist
              current-context: todolist
              users:
                - name: k8s-agent
                  user:
                    tokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
              KUBEEOF
              chmod 600 /data/.kube/config
              # Seed Serena project config.
              mkdir -p /data/workspace/.serena
              printf '%s\n' 'project_name: k8s-agent' 'languages:' '  - bash' '  - yaml' \
                > /data/workspace/.serena/project.yml
              chown -R 1000:1000 /data
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            runAsNonRoot: false
            allowPrivilegeEscalation: false
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 128Mi
          volumeMounts:
            - name: zeroclaw-k8s-data
              mountPath: /data
            - name: config-template
              mountPath: /etc/zeroclaw-template/config.toml
              subPath: config.toml
              readOnly: true
            - name: identity
              mountPath: /etc/zeroclaw-template/identity
              readOnly: true
        - name: git-setup
          image: alpine/git:2.47.2
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - |
              set -e
              mkdir -p /data/.config/git
              cat > /data/.config/git/config <<GITEOF
              [credential]
                  helper = store --file=/data/.config/git/credentials
              [user]
                  name = Zeroclaw K8s Agent
                  email = k8s-agent@scrapyard.local
              [safe]
                  directory = /data/workspace/scrapyard-applications
              GITEOF
              echo "https://dan:${GIT_TOKEN}@gitea.coffee-anon.com" > /data/.config/git/credentials
              chmod 600 /data/.config/git/credentials
              REPO_DIR="/data/workspace/scrapyard-applications"
              if [ -d "${REPO_DIR}/.git" ]; then
                echo "Repository exists, pulling latest..."
                cd "${REPO_DIR}" && git pull --ff-only || echo "Pull failed, continuing with existing state"
              else
                echo "Cloning scrapyard-applications..."
                git clone https://gitea.coffee-anon.com/dan/scrapyard-applications.git "${REPO_DIR}"
              fi
          env:
            - name: GIT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: zeroclaw-k8s-agent-secrets
                  key: git-token
            - name: HOME
              value: /data
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: zeroclaw-k8s-data
              mountPath: /data
      containers:
        - name: zeroclaw
          image: citizendaniel/zeroclaw-sam:v1.4.4
          imagePullPolicy: Always
          command:
            - bash
            - -ec
            - |
              export PATH="/data/bin:$PATH"
              mkdir -p /data/.zeroclaw /data/workspace
              cd /data/workspace
              # Wait for Serena MCP sidecar to be ready.
              echo "Waiting for Serena MCP server on port 9121..."
              for _i in $(seq 1 60); do
                if (echo > /dev/tcp/127.0.0.1/9121) 2>/dev/null; then echo "Serena ready." && break; fi
                sleep 1
              done
              (echo > /dev/tcp/127.0.0.1/9121) 2>/dev/null || echo "Warning: Serena not ready, starting zeroclaw anyway"
              exec zeroclaw daemon
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: HOME
              value: /data
            - name: RUST_LOG
              value: "info,zeroclaw::gateway::acp_server=debug,zeroclaw::agent=debug,zeroclaw::providers=debug"
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: zeroclaw-k8s-agent-secrets
                  key: api_key
            - name: ZEROCLAW_GATEWAY_PAIRED_TOKENS
              valueFrom:
                secretKeyRef:
                  name: zeroclaw-k8s-agent-secrets
                  key: gateway-paired-tokens
                  optional: true
            - name: KUBECONFIG
              value: /data/.kube/config
            - name: GIT_CONFIG_GLOBAL
              value: /data/.config/git/config
            - name: SERENA_SESSION_FILE
              value: /data/.zeroclaw/serena-session-id
            - name: SERENA_DEFAULT_PROJECT
              value: /data/workspace
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            allowPrivilegeEscalation: true
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              memory: 1Gi
          volumeMounts:
            - name: zeroclaw-k8s-data
              mountPath: /data
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
        - name: serena
          image: citizendaniel/zeroclaw-sam:v1.4.4
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -ec
            - |
              mkdir -p /data/workspace
              cd /data/workspace
              exec /opt/sam-tools/home/.local/bin/serena start-mcp-server \
                --transport streamable-http \
                --port 9121 \
                --enable-web-dashboard false \
                --enable-gui-log-window false \
                --project /data/workspace
          ports:
            - name: serena-mcp
              containerPort: 9121
              protocol: TCP
          env:
            - name: HOME
              value: /opt/sam-tools/home
            - name: UV_TOOL_DIR
              value: /opt/sam-tools/home/.local/share/uv/tools
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          volumeMounts:
            - name: zeroclaw-k8s-data
              mountPath: /data
      volumes:
        - name: config-template
          configMap:
            name: zeroclaw-k8s-agent-config
        - name: identity
          configMap:
            name: zeroclaw-k8s-agent-identity
  volumeClaimTemplates:
    - metadata:
        name: zeroclaw-k8s-data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: rook-ceph-block
        resources:
          requests:
            storage: 10Gi
