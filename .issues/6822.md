# [Feature]: Add zeroclaw-tui to release build matrix and package managers

Add `zeroclaw-tui` to the release build matrix, CI pipeline, and package manager distributions so the TUI binary ships alongside the main `zeroclaw` binary.

### Current state

- `zeroclaw-tui` crate exists but is not built in CI or included in releases
- Only reference is in `.github/CODEOWNERS`

### Work

- [ ] Add `zeroclaw-tui` to CI build matrix (all supported platforms: Linux x86_64, Linux aarch64, macOS x86_64, macOS aarch64)
- [ ] Add `zeroclaw-tui` binary to release artifacts (GitHub Releases tarballs)
- [ ] Add to Homebrew formula
- [ ] Add to AUR package
- [ ] Add to Docker image (if applicable)
- [ ] Cross-compilation verification (the TUI may pull in terminal-specific deps like `crossterm` that need platform testing)

### Dependencies

- #6821 — Move crates/zeroclaw-tui to apps/tui (final location must be settled first)

### Parent

- #6826 — ZeroClaw TUI tracker

