# [Feature]: Move crates/zeroclaw-tui to apps/tui

Move `crates/zeroclaw-tui` to `apps/tui` to match the project layout convention where end-user applications live under `apps/` and libraries live under `crates/`.

### Current state

- `crates/zeroclaw-tui/` — TUI library crate with `lib.rs`, `onboarding.rs`, `theme.rs`, `widgets.rs`
- `apps/tauri/` — Tauri desktop app already lives under `apps/`

### Work

- [x] Move `crates/zeroclaw-tui/` to `apps/tui/`
- [x] Update `Cargo.toml` workspace members
- [x] Update any `path = "..."` dependencies referencing the old location
- [ ] Add a `[[bin]]` target (or `src/main.rs`) — the TUI needs to be an executable, not just a library
- [x] Verify `cargo build -p zeroclaw-tui` still works from the new location

### Parent

- #6826 — ZeroClaw TUI tracker
