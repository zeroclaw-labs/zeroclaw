---
phase: 1.2
slug: workspace-skeleton-binary-split
verified: 2026-06-12
status: passed
nyquist_compliant: true
---

# Phase 1.2 — Verification

## Result: ✅ PASSED

3 must-haves derived from the phase goal ship and verify clean. Quick-gate 19/19. One CI gate (workspace-build) will exercise on push.

## Must-haves (goal-backward)

| Must-have | Status | Evidence |
|---|---|---|
| **MH-1** Two-binary workspace topology exists (`bins/engineer`, `bins/wizard`); each binary's `Cargo.toml` is the human-readable manifest of compiled-in capabilities | ✅ | `bins/engineer/Cargo.toml` and `bins/wizard/Cargo.toml` both exist with package names `osagent-engineer` and `osagent-wizard`; binary names `engineer` and `wizard` (drop-in replace for `/usr/local/bin/{engineer,wizard}`); `[dependencies]` blocks empty at M1 (will grow as Phase 1.3+ extracts crates). Workspace `[workspace].members` extended. |
| **MH-2** Explicit-registration discipline ratified | ✅ | Direct grep of v0.7.5 source: 0 instances of `inventory!`/`linkme!`/`ctor!`. deny.toml bans the crates. CI job `no-distributed-slice-registration` enforces on every PR. |
| **MH-3** Structural foundation for wizard-no-MCP property is in place | ✅ | `bins/wizard/Cargo.toml` has a prominent load-bearing comment block at top documenting the rule; quick-gate verifies wizard's Cargo.toml has no MCP dep declaration; Phase 1.3 extracts `osagent-tools-mcp` as a separate crate that wizard will NEVER list. |

## Per-requirement coverage

| REQ-ID | Plan | Status | Acceptance evidence |
|---|---|---|---|
| WS-01 | inline | ✅ | `resolver = "2"` pinned (upstream); `bins/engineer`+`bins/wizard` exist; both ~30 LOC (under 50); `workspace-build` CI job set up to verify `cargo build` on every push. |
| WS-04 | inline | ✅ | deny.toml bans 3 distributed-slice crates; CI source-grep job catches direct invocations; quick-gate scans local tree (0 hits). |
| WS-05 | inline | ✅ (prep) | Convention documented; `[dependencies]` empty at Phase 1.2 so nothing to audit yet. Active enforcement (`cargo tree --duplicates`) lands in Phase 1.5. |

## Validation evidence

### scripts/validate-quick.sh (live run)
```
=== 19 passed, 0 failed ===
```
(Full list in SUMMARY.md.)

### scripts/validate-full.sh
- `cargo deny check` — deferred to CI (no local cargo-deny install)
- `cargo build -p osagent-engineer -p osagent-wizard --release` — will run in `workspace-build` CI job on the next push

## Deferred items (not blocking M1 close)

- `cargo tree --duplicates` audit script → Phase 1.5
- `build.rs` provenance injection → Phase 1.5
- `clap` flags on engineer/wizard → M2
- Strip upstream's `[[bin]] zeroclaw` and `[[bin]] zeroclaw-acp-bridge` → Phase 1.6 / M2

## Sign-off

- ✅ 3 must-haves verified
- ✅ WS-01 + WS-04 + WS-05 (prep) all addressed
- ✅ Quick-gate 19/19
- ✅ CI gates added: `no-distributed-slice-registration`, `workspace-build`
- ✅ Locked decisions from CONTEXT.md honored; discretion areas matched recommendations
- ✅ Hollow-wizard-at-M1 pattern (FEATURES.md) realized

**Phase 1.2 is complete. Proceeding to Phase 1.3.**
