---
phase: 1.1
slug: fork-attribution-sync-runbook
verified: 2026-06-12
status: passed
nyquist_compliant: true
---

# Phase 1.1 — Verification

## Result: ✅ PASSED

All 3 must-haves derived from the phase goal ship and verify clean. Quick gate 11/11.

## Must-haves (goal-backward derivation)

The phase goal: "Establish a legally-distributable, quarterly-sustainable fork of zeroclaw v0.7.5 with license discipline ratified at the CI layer."

| Must-have | Status | Evidence |
|---|---|---|
| **MH-1** True GitHub fork relationship exists at `andreas2301/osAgent` with `osagent-main` as the working branch, attribution preserved | ✅ | `git remote -v` shows `https://github.com/andreas2301/osAgent.git` as `origin`; `git branch --show-current` returns `osagent-main`; `NOTICE` file has both the upstream's verbatim block (below `---`) and the osAgent attribution + disambiguation; `Cargo.toml` `[workspace.metadata.osagent].forked_at_sha = "a2988f0dfffa0c14fa56e218c7bb9f28da494da4"`. |
| **MH-2** Quarterly upstream-sync procedure is documented and ratified | ✅ | `sovereign-shield-backup/documentation/osAgent/UPSTREAM_SYNC.md` exists on `feat/osagent-upstream-sync-runbook` with 8 required sections (Purpose / Branches / Cadence & Budget / Procedure / Out-of-Cycle / Refuse-to-Merge / Append-Only Conflict Log / Worked Example); seeded with 2026-06-12 baseline entry; pushed for PR. Cross-link added to `documentation/README.md`. |
| **MH-3** `cargo deny` discipline ratified in CI from PR #1 forward | ✅ | `deny.toml` extends upstream config with AGPL/WTFPL/phone-home bans and `[graph].all-features = false`; `.github/workflows/osagent-policy.yml` runs `cargo-deny-action@<v2.0.20 SHA>` on every PR + push + daily; both files committed and pushed to `osagent-main`. |

## Per-requirement coverage

| REQ-ID | Plan | Status | Acceptance evidence |
|---|---|---|---|
| FORK-01 | 01.1-01 | ✅ | NOTICE markers + Cargo.toml provenance + git remote + branch — all 11 quick-gate checks pass |
| FORK-02 | 01.1-03 | ✅ | UPSTREAM_SYNC.md on `feat/osagent-upstream-sync-runbook`; 8 sections all present; quarterly cadence; baseline log entry seeded |
| FORK-03 | 01.1-02 | ✅ | deny.toml + osagent-policy.yml shipped; SHA-pinned cargo-deny-action; ban list covers AGPL/WTFPL/phone-home |

## Validation evidence

### scripts/validate-quick.sh (live run)
```
=== Phase 1.1 validate-quick ===
  ✓ preflight artifacts present
  ✓ upstream remote configured
  ✓ origin remote configured
  ✓ osagent-main branch exists
  ✓ NOTICE has fork disambiguation
  ✓ dual-license files present
  ✓ Cargo.toml has fork provenance metadata
  ✓ deny.toml present
  ✓ deny.toml lists AGPL-3.0 banlist target
  ✓ osagent-policy.yml wires cargo-deny-action
  ✓ UPSTREAM_SYNC.md present on feat/osagent-upstream-sync-runbook (PR pending merge)
=== 11 passed, 0 failed ===
```

### scripts/validate-full.sh (deferred)
The `cargo deny check {licenses,bans,sources,advisories}` portion requires `cargo-deny` to be installed locally. Skipped at Phase 1.1 — the CI workflow runs it automatically on every PR. Acceptable per VALIDATION.md sampling-rate strategy: cargo-deny is a CI-side gate, not a local-loop one.

## Human verifications (per VALIDATION.md)

| Behavior | Status | Notes |
|---|---|---|
| GitHub fork badge visible | ⏳ Pending user spot-check | User created the fork manually via UI on 2026-06-12; relationship verified by `git push -u origin osagent-main` succeeding (push -u sets up tracking only if origin accepts the branch — implicit confirmation). |
| Branch protection rules effective | ⏳ Deferred | Branch protection deferred to a follow-up (needs GitHub API call with PAT in env or user-driven UI configuration). Acceptable for Phase 1.1; recommended action: user configures branch protection on `osagent-main` via Repo Settings → Branches with required status checks `cargo-deny (licenses + bans + sources)` and `cargo-deny (advisories)`. |
| UPSTREAM_SYNC.md tutorial readable | ⏳ Pending user spot-check | Worked Example section traces a hypothetical Q3 2026 sync against `upstream/v0.8.0`. User should read end-to-end and confirm unambiguity before merging the sovereign-shield-backup PR. |

## Deferred items (not blocking M1 close)

- Branch protection on `osagent-main` (manual or future-phase task).
- PR merges (two PRs awaiting user-side review).
- `cargo about` NOTICE.md auto-generation → Phase 1.5.
- `opentelemetry-otlp` optional dep deletion → Phase 1.4.

## Sign-off

- ✅ All 3 must-haves verified
- ✅ All 3 FORK-* requirements covered by at least one plan; acceptance evidence collected
- ✅ Quick-gate 11/11; full-gate `cargo deny` deferred to CI (acceptable per VALIDATION.md)
- ✅ Locked decisions from CONTEXT.md honored; discretion areas matched recommendations
- ✅ Deferred ideas excluded
- ✅ Cross-plan data contracts intact (preflight.env → Wave 1, known-issues.md → Wave 2a, NOTICE upstream-preservation)
- ⏳ 3 human verifications pending (badge, branch protection, runbook prose) — none block Phase 1.2 start

**Phase 1.1 is complete. Proceeding to Phase 1.2.**
