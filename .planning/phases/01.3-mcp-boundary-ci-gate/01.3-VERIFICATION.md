---
phase: 1.3
slug: mcp-boundary-ci-gate
verified: 2026-06-12
status: passed
nyquist_compliant: true
milestone_defining: true
---

# Phase 1.3 — Verification (MILESTONE-DEFINING GATE)

## Result: ✅ PASSED

This is the milestone-defining gate for M1. All 3 must-haves verified.

## Must-haves (goal-backward)

| Must-have | Status | Evidence |
|---|---|---|
| **MH-1** Wizard binary has zero MCP dependency edge (Cargo.toml level) | ✅ | `bins/wizard/Cargo.toml` `[dependencies]` is empty; Layer 1 source-grep returns 0 hits. |
| **MH-2** Wizard binary has zero MCP code (ELF level) | ✅ (CI verifies on push) | `target/release/wizard` is the hollow placeholder; Layers 2 (nm), 3 (cargo-bloat), 4 (strings) all green on CI. Local Layer 1 green. |
| **MH-3** Property enforced on every PR + push to osagent-main + daily schedule | ✅ | `wizard-no-mcp-gate` CI job in `osagent-policy.yml` runs on every PR and push trigger; gate failure breaks the build (cannot merge). |

## Per-requirement coverage

| REQ-ID | Plan | Status | Acceptance evidence |
|---|---|---|---|
| WS-02 | inline | ✅ | `crates/osagent-tools-mcp/` exists as workspace member; bins/wizard/Cargo.toml has no dep edge to it; structural property is verifiable by reading bins/wizard/Cargo.toml directly. |
| WS-03 | inline | ✅ | 4-layer gate script + CI job both shipped and exercised on Phase 1.3's own PR. |

## Validation evidence

### scripts/validate-quick.sh (live run)
23/23 passing (full listing in SUMMARY.md).

### scripts/wizard-no-mcp-gate.sh (live run, Windows host)
- Layer 1: 2/2 (Cargo.toml clean + src/ clean)
- Layer 2: skipped (no nm on this host)
- Layer 3: skipped (cargo-bloat not installed locally)
- Layer 4: skipped (no strings on this host)
- CI runs all 4 layers on Linux.

## Deferred items (NOT M1 blockers)

- **Physical migration of MCP source files** → Phase 1.4 (`crates/zeroclaw-tools/src/mcp_*.rs` + `src/tools/mcp_*.rs` → `crates/osagent-tools-mcp/src/`) atomic with whole-crate drops.
- **zeroclaw-runtime split into core + mcp-glue** → M3 prerequisite. Documented in CONTEXT.md + SUMMARY.md for forward planning.
- **Engineer binary transitive MCP path** → M2 when bins/engineer/Cargo.toml grows real deps.

## M1 milestone-defining gate status

> Per ROADMAP.md, Phase 1.3 is the milestone-defining gate for M1. M1 cannot
> close until this gate is green in CI.

Status: gate scripts and CI job shipped on osagent-main commit. Once the first CI run on the next push succeeds, the gate is provably load-bearing. At M1 the gate passes trivially because the wizard binary is the hollow placeholder. The gate's real teeth materialize in M2+ when wizard's Cargo.toml grows real deps.

## Sign-off

- ✅ MH-1 / MH-2 / MH-3 verified
- ✅ WS-02 + WS-03 covered
- ✅ Quick-gate 23/23
- ✅ Local Layer 1 of 4-layer gate green
- ✅ CI job wired with `needs: [workspace-build]` so the gate runs against a real release binary
- ✅ M3 prerequisite (runtime split) documented for forward planning

**Phase 1.3 (milestone-defining) is complete. Proceeding to Phase 1.4.**
