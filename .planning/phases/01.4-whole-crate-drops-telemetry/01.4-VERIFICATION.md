---
phase: 1.4
slug: whole-crate-drops-telemetry
verified: 2026-06-12
status: passed (with deferred-source-strip caveat)
nyquist_compliant: true
---

# Phase 1.4 — Verification

## Result: ✅ PASSED (CI workspace-build may fail until Phase 1.5; expected and accepted)

## Must-haves

| Must-have | Status | Evidence |
|---|---|---|
| **MH-1** 5 dead crates physically removed | ✅ | `find crates apps -maxdepth 2 -type d -name "zeroclaw-hardware" -o -name "robot-kit" -o -name "aardvark-sys" -o -name "zeroclaw-plugins" -o -name "tauri" \| wc -l` returns 0. ~16.2K LOC removed. |
| **MH-2** Webhook channel source + feature removed | ✅ | `webhook.rs` gone; `pub mod webhook` declaration gone; `channel-webhook` feature definition gone. |
| **MH-3** Phone-home transports audited and removed | ✅ | Audit findings: only `opentelemetry-otlp` was present as an optional dep; now deleted along with the `observability-otel` feature. No sentry/posthog/honeycomb/datadog. |
| **MH-4** MCP physical migration into osagent-tools-mcp (Phase 1.3 deferred work) | ✅ | 5 files in canonical location; lib.rs has real `pub mod` declarations; consumers (zeroclaw-runtime) updated to import from new location. |

## Per-requirement coverage

| REQ-ID | Plan | Status | Acceptance evidence |
|---|---|---|---|
| STRIP-01 | inline | ✅ | 5 crate dirs deleted; workspace.members updated; downstream Cargo.toml deps cleaned in 3 crates (runtime, gateway, root). |
| STRIP-06 | inline | ✅ | Webhook file + mod + feature triple-deleted. |
| TELEMETRY-01 | inline | ✅ | Audit findings documented; OTLP optional dep removed. |

## Caveat (NOT M1 blocker)

Phase 1.4 deletes the dead crates + cleans Cargo.toml dep edges, but does NOT yet strip every source-level reference to the dropped crates (e.g., `use crate::hardware::*` in `src/agent/` files, `#[cfg(feature = "plugins-wasm")]` blocks in zeroclaw-runtime). Phase 1.5 owns those source strips — the roadmap explicitly splits whole-crate-drops (1.4) from source-strips (1.5).

**Consequence**: `workspace-build` CI job may fail until Phase 1.5 completes. The milestone-defining `wizard-no-mcp-gate` (Phase 1.3) remains green throughout — wizard binary is hollow.

## Sign-off

- ✅ All must-haves verified
- ✅ STRIP-01 + STRIP-06 + TELEMETRY-01 covered
- ✅ Quick-gate 23/23
- ⚠ Workspace-build deferred green until Phase 1.5 completes source strips (per roadmap design)
- ✅ MCP file migration ships Phase 1.3's deferred work

**Phase 1.4 is complete. Proceeding to Phase 1.5 to clean cascading source-level references.**
