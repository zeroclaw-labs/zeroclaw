---
name: 2026-04-17 Handoff — Workspace Verification & Warning Cleanup
description: 다음 세션이 이어받아 전수 검증 + 경고 정리를 마칠 수 있도록 정리한 인계 노트
type: handoff
generated_at: 2026-04-17
branch: feat/document-pipeline-overhaul
last_commit: 00f98e3f docs(arch): §6E-9-A/B/C — PR-by-PR architecture detail + impact + follow-up tracks
---

# 다음 세션 인계 — 전수 검증 & 경고 정리

## 1. 확정된 사실 (재검증 없이 신뢰 가능)

- `cargo check --lib` ✅ 통과 (exit 0, 0 errors, 24 warnings)
- `cargo check --all-targets` ✅ 통과 (bin + tests 컴파일됨)
- **컴파일 에러 0건** — 문제는 전부 warning 레벨

## 2. 이번 세션에 검증하지 못한 것 (다음 세션의 첫 업무)

| 작업 | 상태 | 실행 커맨드 |
|---|---|---|
| `cargo test --lib` 전수 | ❌ 미실행 | `cargo test --lib` (컨텍스트 제약으로 skip) |
| 모듈별 회귀 | ❌ 미실행 | `cargo test --lib memory:: sync:: phone:: vault:: skills:: user_model:: ontology::` |
| 통합 테스트 | ❌ 미실행 | `cargo test --test '*'` |
| `cargo clippy -D warnings` | ❌ 미실행 | `cargo clippy --all-targets -- -D warnings` |

## 3. 수정해야 할 경고 목록 (24건, 파일별 정리)

### 3.1 잠재적 로직 버그 (우선 수정)

| 위치 | 경고 | 진단 |
|---|---|---|
| `src/sync/hlc.rs:158` | `unexpected cfg condition value: 'test-util'` | 거의 확실히 오타 — `#[cfg(test-util)]`는 절대 매치되지 않아 **dead code**. `#[cfg(feature = "test-util")]` 또는 `#[cfg(any(test, feature = "test-util"))]`로 고칠 것. Cargo.toml에 `test-util` 피처 정의 필요 여부도 확인. |
| `src/vault/wikilink/ai_stub.rs:155:57` | `unreachable pattern` (2건) | match arm 중 앞선 패턴이 이미 모든 값을 소진함. 155번 줄 전후의 `match` 블록을 읽고 중복/포괄 패턴 제거. |

### 3.2 unused imports (14건 — 단순 삭제)

```
src/memory/dream_cycle.rs:471:54      — ConsolidationOutcome
src/memory/sqlite.rs:1961:13          — super::embedding::EmbeddingProvider
src/memory/sqlite.rs:2060:13          — super::embedding::EmbeddingProvider
src/phone/caller_match.rs:9:13        — Deserialize
src/session_search/mod.rs:13:9        — lifecycle::SessionHandle
src/session_search/mod.rs:14:17       — ChatMessage, ChatSession, SessionSearchHit
src/skills/correction/mod.rs:22:19    — UserAction, apply_feedback
src/skills/correction/mod.rs:24:27    — ValidationResult, ValidationVerdict, validate_correction
src/skills/correction/mod.rs:25:20    — CorrectionObservation, observe_edit
src/skills/correction/mod.rs:26:25    — PatternUpdate, mine_patterns
src/skills/correction/mod.rs:27:43    — CorrectionRecommendation
src/skills/correction/mod.rs:28:17    — CorrectionPattern, PatternType
src/skills/procedural/mod.rs:21:9     — auto_create::maybe_create_skill
src/skills/procedural/mod.rs:23:21    — build_prompt_injection, should_trigger
src/skills/procedural/mod.rs:24:23    — SkillDepth, SkillSummary, inject_skill_index
src/skills/procedural/mod.rs:25:9     — self_improve::improve_after_execution
src/skills/procedural/mod.rs:27:9     — sync::SkillUpsertDelta
src/user_model/mod.rs:15:20           — ProfileConclusion, UserProfiler
src/vault/hub.rs:1128:65              — HeuristicAIEngine (test only)
src/workflow/scaffold.rs:245:9        — super::parser::Step
```

**주의**: `skills/procedural/mod.rs`와 `skills/correction/mod.rs`의 대량 unused imports는
public API re-export 의도였을 가능성이 큼. `#[allow(unused_imports)]` 대신 진짜 re-export
(`pub use …::{Foo, Bar};`)인지 확인 필요. 단순 `use` 문이면 삭제, `pub use`로 의도된 거면 유지.

### 3.3 unused variables / non-snake-case (소소)

```
src/memory/sqlite.rs:4586-4587   — idA/idB → id_a/id_b (테스트 코드)
src/phone/context_inject.rs:58   — key → _key
src/tools/hwpx_create.rs:339     — tool → _tool (테스트 코드)
```

## 4. 최근 커밋 기준 주요 변경 영역 (참고용)

최근 15개 커밋에서 손댄 영역:
- `src/memory/embedding/` — 로컬 BGE-M3 통합 (PR #1)
- `src/memory/{consolidate, decay, search}.rs` — PR #6
- `src/sync/hlc.rs` — PR #7 (← 이번 cfg 오타 의심 파일)
- `src/skills/{procedural, correction}/` — v6.1 자기학습 시스템
- `src/user_model/` — 사용자 프로파일러
- `src/session_search/` — 세션 검색 (§6F)
- `src/vault/scheduler.rs` — PR #9 community detection
- `src/memory/synced.rs` — HLC stamp 기반 accept_remote_store_if_newer

## 5. working tree 상태 (commit 안 된 변경)

```
M src/memory/sqlite.rs           (+77 lines: SkillUpsert / UserProfileConclusion / CorrectionPatternUpsert delta handlers)
M src/skills/correction/store.rs (+10 lines)
M src/skills/procedural/store.rs (+18 lines)
M src/sync/protocol.rs           (+44 lines)
```

**이 4개 파일의 변경을 먼저 검토하고 테스트가 통과하는지 확인한 뒤에 커밋해야 함.**
변경 내용 요약: 자기학습 skill 시스템의 델타 동기화 핸들러가 추가되었고,
`DeltaOperation`에 `SkillUpsert`, `UserProfileConclusion`, `CorrectionPatternUpsert` 변형이
들어간 것으로 보임. 이게 `sync/protocol.rs::merge_deltas_lww`의 dedup 키 매치에서
exhaustive 매치를 깨뜨리고 있지 않은지(새 variant 추가 시 흔한 실수) 확인 필요.

## 6. 다음 세션 작업 순서 (권장)

1. `git status` + `git diff` — 5번의 untracked/modified 파일 내용 확인
2. `cargo test --lib 2>&1 | tee /tmp/test-full.log` — 전수 테스트 (10분+ 예상)
3. `grep "FAILED\|test result" /tmp/test-full.log` — 실패 점검
4. 3.1 로직 버그 수정 (hlc.rs cfg + ai_stub.rs unreachable)
5. 3.2 unused imports 일괄 정리 (단, skills/ 아래는 pub use 의도 확인)
6. 3.3 snake_case / unused var 리네임
7. `cargo clippy --all-targets -- -D warnings` 재실행 — 0 warnings 목표
8. 통과 후 4번의 working tree 변경 커밋 (1~2개 원자 커밋으로 분리)

## 7. 위험 요소

- `src/memory/sqlite.rs`의 77줄 추가는 새 Delta variant를 처리하는데, 해당 variant가
  `src/memory/sync.rs`의 `DeltaOperation` enum에 실제 정의되어 있는지 먼저 확인.
  없다면 컴파일은 통과해도 런타임에 핸들러가 절대 호출되지 않는 dead code임.
- `hlc.rs:158`의 `cfg(test-util)`은 해당 블록이 지금 **어떤 빌드에서도 포함되지 않음**을
  의미. 만약 거기에 테스트 헬퍼가 있으면 테스트가 그 헬퍼를 쓰는 순간 실패함.

끝.
